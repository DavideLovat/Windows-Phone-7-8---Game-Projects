using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace ZoneGame
{
    public class AttackBehavior : Behavior
    {
        public AttackBehavior(Actor actor)
            :base(actor)
        {
        }

        public override void Update(Actor otherAnimal, AIParameters aiParams)
        {
            
        }
    }
}
